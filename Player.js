class Player {

    constructor(x,y,width,height) {

        var options = {
            isStatic:false,
            friction:0.001
        }

        this.height = height;
        this.width = width;
        this.body = Bod
    }
}